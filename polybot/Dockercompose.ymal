version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081"
    volumes: 
      - .:/app

  web:
    build:
      context: .
      dockerfile: Dockerfile.Nginx
    ports:
      - "80:80"